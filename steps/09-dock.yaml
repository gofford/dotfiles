# Dock configuration — run explicitly with: make dock
# WARNING: This wipes the Dock and rebuilds from scratch.
- shell:
    - description: Configure Dock with dockutil
      command: scripts/configure-dock.sh
